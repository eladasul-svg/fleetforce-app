<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>In_Shop__c</fullName>
    <formula>/* Logic: Ticket has started AND (Ticket end is blank OR Ticket end is in the future) */
AND(
    NOT(ISBLANK(Actual_Start__c)),
    OR(
        ISBLANK(Actual_End__c),
        Actual_End__c &gt; TODAY()
    )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>In Shop</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
